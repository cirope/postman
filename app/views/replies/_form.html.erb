<%= simple_form_for [@ticket, @reply] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :body, input_html: { autofocus: true, rows: 4 } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
